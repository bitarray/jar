[package]
name = "nub-build"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "build.rs helpers: cross-compile nub guest crates (PVM2 programs, bare-metal arch guest kernels)"

[dependencies]
build-crate = { path = "../../rust/build-crate" }
nub-linker = { path = "../nub-linker" }
nub-program = { path = "../nub-program" }
