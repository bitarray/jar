/// Run the merge workflow for a PR.
pub fn run(_pr: u64, _founder_override: bool) -> Result<(), Box<dyn std::error::Error>> {
    // TODO: implement
    eprintln!("workflow merge: not yet implemented");
    std::process::exit(1);
}
