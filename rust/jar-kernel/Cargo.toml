[package]
name = "jar-kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "JAR v3 kernel: σ state, block apply, state-root, kernel-assisted Instances."

[dependencies]
javm-cap = { workspace = true }
javm-exec = { workspace = true }
javm = { workspace = true }
scale = { workspace = true, features = ["std"] }
scale-derive = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[build-dependencies]
build-javm = { path = "../build-javm" }
