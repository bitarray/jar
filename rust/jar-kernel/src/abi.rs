//! Slot numbers the v3 chain ABI exposes to chain Instances at
//! genesis. Sourced from [`jar_cap::abi`] so the transpiler can
//! reference the same constants when building chain Images.

pub use jar_cap::abi::*;
