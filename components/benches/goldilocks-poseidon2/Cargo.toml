[package]
name = "bench-goldilocks-poseidon2"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_env, values("javm"))', 'cfg(target_os, values("none"))'] }
