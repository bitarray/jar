[package]
name = "simple-chain"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Example JAR chain (v3 placeholder; runnable end-to-end demo lives in jar-kernel integration tests)."

[dependencies]
